import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'api.demo.ionic.vue',
  appName: 'api-demo-ionic-vue',
  webDir: 'dist',
  bundledWebRuntime: false
};

export default config;
